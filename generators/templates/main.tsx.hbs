import { NestFactory } from "@nestjs/core"
import { {{pascalCase name}} } from './{{name}}.module'

async function bootstrap() {
    const app = await NestFactory.create({{pascalCase name}}Module)
    await app.listen(3000)
}

bootstrap()